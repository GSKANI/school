# CoreSchool (minimal dev setup)

This workspace contains a minimal Vite + React setup to run the `CoreSchool` demo UI.

Quick start:

```powershell
npm install
npm start
```

The main app component is at `src/CoreSchool.jsx` and the entry file is `src/main.jsx`.
